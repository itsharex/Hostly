name: Publish

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  publish-tauri:
    permissions:
      contents: write
      id-token: write # Sometimes needed for certain OIDC/signing ops, harmless to add
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest] # Only building for Windows as requested

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: 'src-tauri'

      - name: install dependencies (frontend)
        run: npm install

      - name: Delete existing release
        uses: dev-drprasad/delete-tag-and-release@v1.0
        with:
          delete_release: true
          tag_name: releases
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Release
        uses: tauri-apps/tauri-action@v0
        id: tauri-build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: releases
          releaseName: 'Releases'
          releaseBody: |
            This is the latest build.
            
            **Downloads:**
            See assets below.
          releaseDraft: false
          prerelease: false

      - name: Upload Portable EXE
        uses: softprops/action-gh-release@v1
        if: success()
        with:
          tag_name: releases
          files: src-tauri/target/release/hosts-switcher.exe
          token: ${{ secrets.GITHUB_TOKEN }}
